apiVersion: v1
kind: ConfigMap

metadata:
  name: employee-config
  namespace: employee-management

data:
  DB_HOST: "mysql-service"
  DB_NAME: "employee_management"
  PORT: "3010"
  NODE_ENV: "production"
